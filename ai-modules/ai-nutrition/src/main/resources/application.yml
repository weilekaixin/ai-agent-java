server:
  port: 4001

spring:
  application:
    name: ai-nutrition
  profiles:
    active: ${profiles.active:dev}

--- # nacos 配置
spring:
  cloud:
    nacos:
      server-addr: ${NACOS_ADDR:127.0.0.1:8848}
      username: ${NACOS_USERNAME:nacos}
      password: ${NACOS_PASSWORD:nacos}
      discovery:
        group: ${nacos.discovery.group:DEFAULT_GROUP}
        namespace: ${spring.profiles.active}
      config:
        group: ${nacos.config.group:DEFAULT_GROUP}
        namespace: ${spring.profiles.active}
    sentinel:
      transport:
        dashboard: ${SENTINEL_ADDR:127.0.0.1:8080}
  config:
    import:
      - optional:file:.env[.properties]
      - optional:nacos:application-common.yml
      - optional:nacos:${spring.application.name}.yml

--- # 数据源配置
spring:
  datasource:
    url: jdbc:postgresql://${DB_HOST:localhost}:${DB_PORT:5432}/ai_nutrition_db
    username: ${DB_USERNAME:postgres}
    password: ${DB_PASSWORD:postgres}
    driver-class-name: org.postgresql.Driver

--- # RocketMQ 配置
rocketmq:
  name-server: ${ROCKETMQ_ADDR:localhost:9876}
  producer:
    group: ${spring.application.name}

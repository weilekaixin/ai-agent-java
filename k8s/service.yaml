apiVersion: v1
kind: Service
metadata:
  name: ai-agent-java
  namespace: ai-agent
  labels:
    app: ai-agent-java
spec:
  selector:
    app: ai-agent-java
  ports:
    - name: http
      port: 80
      targetPort: 8080
      protocol: TCP
  type: ClusterIP
